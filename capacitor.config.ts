import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.goktugecikli.mobilefootballcareer',
  appName: 'Mobile Football Career',
  webDir: 'dist',
};

export default config;
