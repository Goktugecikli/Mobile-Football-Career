---
description: Design tokens and UI styling rules
globs: "**/*.{css,tsx}"
alwaysApply: false
---

# UI and design tokens

`src/styles/tokens.css` is the authoritative source for colors, typography, spacing, layout, safe-area, radii, shadows, motion, z-index, and breakpoints.

Application components must not introduce arbitrary colors, font sizes, or spacing when a token already exists. Add a token when the value is a repeated visual decision, not when it is a one-off implementation detail.

Use semantic token names (`--color-text-primary`, `--space-md`), not raw palette nicknames, in component CSS. Palette tokens (`--palette-*`) are internal to `tokens.css` only.

Prefer CSS modules colocated with the component that owns them. Global styles belong in `src/styles` and should stay limited to reset, root document rules, and token application.

When JS needs breakpoint values, keep them aligned with the numbers documented in `tokens.css`. CSS custom properties cannot reliably drive `@media` queries.

Consume `--layout-padding-*` and `--safe-area-inset-*` for mobile insets. Do not repeat `env(safe-area-inset-*)` in components. Orientation policy lives in `src/config/orientationPolicy.ts`; web layouts must work in portrait and landscape until a product lock is defined.
